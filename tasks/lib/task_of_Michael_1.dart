

import 'package:flutter/material.dart';

class TextWithDollar extends StatelessWidget {
  final String title;
  const TextWithDollar({super.key, required this.title});
   @override
  Widget build(BuildContext context) {
    return Center(
      child: TextFormField(),
    );
  }
}
